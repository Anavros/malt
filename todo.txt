
TODO
    -> clean up the new complex select function and helpers
    -> create a standalone gui version called gmalt
    -> implement logging errors to a file
    -> allow different levels of importance on outputs
    -> improve arrow history scrolling with readline
    -> prevent text from passing a certain number of characters in width
    -> come up with some sweeter names
    -> implement indent() and undent() using context handlers
