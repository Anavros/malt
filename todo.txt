
Bugs:
-> Unknown commands in malt.load()'ed files throw KeyErrors.
-> Special characters are not allowed on input, even in quotes.
-> Helper functions/global functions and error printing is not clear on use.
-> response == str is deprecated, use response.head == str.
-> Printing more than one item doesn't work right.

-> Ambiguity between usage for serve, log, and print.
-> Overcomplication of new printing method.
-> Serve too verbose for many uses.

New Features:
-> Color printing.
-> Tab completion!
-> Helper functions to make testing easier.
-> Better unit tests.
-> There should be a built-in help function. e.g. cmd? gives help for cmd.
-> More fully-featured header.
-> Allow a log message to have more than one level?
    * i.e. use LOG to print and also MIRROR to log to file in one call.
-> Right-justified text formatting.
-> Allow a default value for dict inputs, e.g. take salt -> {salt:1}
-> Allow user input to omit brackets in list and dict input, use inference
-> Allow commenting on options for detailed help.

Recent Changes:
-> Removed blessings.
-> Added autoclear and headers.
-> Added log redirection.
-> **Implemented new parser.
